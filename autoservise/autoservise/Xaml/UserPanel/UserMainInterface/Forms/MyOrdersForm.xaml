<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="autoservise.Xaml.UserPanel.UserMainInterface.Forms.MyOrdersForm">
  <ContentView.Content>
        <StackLayout>
            <Label Text="Мои заказы"
                                   Margin="10"
                                   FontSize="Title"
                                   TextColor="#66C7F4"
                                   />
            <Button Text="Новый заказ"
                                    FontSize="Medium"
                                    TextColor="Black"
                                    BackgroundColor="#EEF6FE"
                                    Padding="15"
                                    Margin="10"/>
            <ScrollView IsVisible="true">
                <StackLayout x:Name="ScrollContent">
                    
                    
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentView.Content>
</ContentView>